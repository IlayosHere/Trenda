---
description: Python code style and quality rules
globs: ["**/*.py"]
---

- Strict PEP 8 and mandatory type hints.
- `snake_case` (vars/functions), `PascalCase` (classes).
- No `print()`; use project logger only.
- No unused imports or dead code.
- Explicit exception handling and deterministic functions preferred.
